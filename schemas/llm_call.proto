syntax = "proto3";
package aegis.v5;

// llm_call — scaffold; fills in Phase 1.
message Llm_call {
  uint32 schema_version = 1;
  uint64 timestamp_ns = 2;
  string origin = 3;
  bytes payload = 10;
}
