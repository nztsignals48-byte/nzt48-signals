# Grafana Datasources Configuration
# Auto-provisioned on container startup

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: "10s"
    editable: true
    orgId: 1
